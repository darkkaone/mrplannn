
class Todo{
  String uid;
  String title;
  bool isComplet;
  int ivalue;
  

  Todo({this.uid, this.title, this.isComplet, this.ivalue});
}

class Percentage{
  double percentage;
  Percentage({this.percentage});
}