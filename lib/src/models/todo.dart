
class Todo{
  String uid;
  String title;
  bool isComplet;
  int ivalue;
  bool pulsado;
  

  Todo({this.uid, this.title, this.isComplet, this.ivalue, this.pulsado});
}

class Percentage{
  int percentage;
  Percentage({this.percentage});
}